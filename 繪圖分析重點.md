繪圖分析重點

一樣，所有的內容放在 temp 目錄，包括程式碼與輸出

------





# **PRD：YOLOv7 1B1H vs 1B4H 訓練診斷（Data & Plot Only）**







## **1. 背景與痛點**





- 目前有多組訓練 run（例如：1B1H、1B4H-AntiConf、1B4H-StrategyA）。

- 觀察到：1B1H 的 obj loss 持續改善，但 1B4H 的 obj loss 幾乎呈現直線；mAP 出現 plateau 或極慢爬升。

- 需要一套 **只靠既有 log / results / checkpoint（可選）** 的分析工具：

  

  - 快速驗證：是否是「真卡住」還是「尺度/分母/繪圖造成的假象」
  - 把現象拆到：mAP、obj、precision/recall、rolling gain、best-after-degrade、EMA 平滑、mAP vs obj 相關性
  - 輸出可直接貼到實驗報告的圖與結論摘要（markdown）

  







## **2. 目標（Goals）**





1. **純離線**：不改訓練程式、不需要重新訓練即可產出分析結果

2. **通用解析**：支援 YOLOv7 常見兩種來源

   

   - results.txt 格式
   - train.log console log 格式（含 ...、含 0/499 0.346G ... P R mAP50 mAP50-95 lr0 lr1 lr2）

   

3. **對齊比較**：多個 run 自動對齊 epoch，生成一致的比較圖

4. **關鍵診斷圖**（至少 6 張）：

   

   - mAP 曲線對比（含 target 水平線）
   - obj/box/cls loss 曲線對比
   - mAP 差值（StrA - Baseline）柱狀
   - rolling gain（10/50/100 epochs）判斷 plateau
   - best-epoch 之後相對退化曲線（mAP - best）
   - mAP vs obj loss scatter（判斷 obj 改善是否真正帶動 mAP）
   - （可選）EMA 平滑曲線

   

5. **定量摘要**：

   

   - 每個 run：best mAP、best epoch、final mAP、final losses、plateau 起點估計
   - 兩兩比較：平均差、後段（例如 last 100 epoch）差異

   

6. **可選進階**：checkpoint BN 統計漂移檢查（best.pt vs last.pt running_mean/var diff）







## **3. 非目標（Non-Goals）**





- 不修改 YOLOv7 訓練流程、不介入 loss 計算方式
- 不做模型推論重跑（除非使用者另外提供 val 輸出）
- 不做自動超參搜尋或自動修模型







## **4. 使用者故事（User Stories）**





- 作為研究者，我想輸入三個 run 的 results/log，就能得到一份圖＋摘要報告，快速回答：

  

  - 1B4H obj loss 直線是真的卡住嗎？
  - plateau 從哪個 epoch 開始？
  - obj loss 的改善是否與 mAP 相關？

  

- 作為工程決策者，我想有一份「可引用的 markdown 報告」放進 EXPERIMENT_REPORT，避免被質疑不公平或結論不可靠。







## **5. 輸入/輸出規格**







### **輸入（Inputs）**





- 必填：每個 run 至少一個檔案（擇一）

  

  - results.txt
  - train.log（console log）

  

- 建議：run 的 meta 資訊（可用 run.yaml 或 CLI 參數給）

  

  - run_name（例如 1B1H、1B4H_StrA）
  - batch_size
  - dataset_size（預設 COCO train2017 = 118287）
  - total_epochs（或從資料推斷）

  

- 可選：checkpoint（用於 BN 漂移檢查）

  

  - best.pt、last.pt

  







### **輸出（Outputs）**





- out/ 目錄下：

  

  - summary.md（含表格與結論）
  - plots/ 一堆 png（每張圖單獨一張，不要子圖拼太小也可；或提供一張 dashboard）
  - metrics.csv（對齊後的 epoch×run 指標表）
  - diffs.csv（pairwise diff 表）
  - （可選）bn_drift.json（BN 漂移統計）

  







## **6. 成功驗收標準（Acceptance Criteria）**





1. 對同一批輸入檔，每次執行輸出一致（deterministic）

2. results.txt 與 console log 任一種輸入都能成功解析

3. 遇到 ...、欄位數不同、尾端有 lr 時不會解析錯位

4. 至少產出：

   

   - mAP 對比圖、obj loss 對比圖、rolling gain 圖、mAP vs obj scatter、best-relative 圖

   

5. summary.md 需包含：

   

   - best mAP、best epoch、final mAP、plateau 診斷（用 gain_100 < threshold）

   

6. 若提供 best/last checkpoint，能輸出 BN 漂移的量化指標





------





# **SDD：系統設計規格（System Design Document）**







## **1. 架構概覽**





採用「資料管線」設計：



1. **Ingest / Parse**

   

   - 讀取每個 run 的檔案（results 或 log）
   - robust parser 轉成 EpochMetrics 序列

   

2. **Align / Normalize**

   

   - 依 epoch 對齊多個 run（交集或聯集可配置）
   - 計算衍生特徵：rolling gains、EMA、best-relative、obj_per_step 等

   

3. **Analysis**

   

   - plateau 偵測：gain_100、線性回歸斜率（可選）
   - correlation：corr(mAP, obj)、scatter trend（可選）
   - pairwise diffs：StrA - Anti / StrA - 1B1H

   

4. **Visualization**

   

   - matplotlib 產圖（不要 seaborn）
   - 每張圖獨立輸出 PNG
   - 統一字型、軸標籤、legend

   

5. **Reporting**

   

   - 產生 summary.md：表格＋要點結論
   - 另存對齊後 csv 方便二次分析

   







## **2. 資料模型（Data Models）**







### **EpochMetrics**





- epoch: int
- box: float | None
- obj: float | None
- cls: float | None
- P: float | None
- R: float | None
- mAP50: float | None
- mAP5095: float | None
- lr0/lr1/lr2: optional
- raw_line: str (debug 用)







### **RunConfig**





- name: str
- input_path: str
- input_type: auto | results | log
- batch_size: int | None
- dataset_size: int = 118287
- total_epochs: int | None
- target_map50: float | None (例如 0.4353)
- color/marker: optional







## **3. 解析策略（最重要）**







### **A) results.txt**





常見欄位：

epoch box obj cls total labels P R mAP50 mAP50-95



- 取末四個 float 當作 P/R/mAP50/mAP50-95
- box/obj/cls 用固定位置（epoch 後第 1~3 個）但仍需容錯







### **B) console log（含** 

### **...**

### **）**





常見結尾：

... P R mAP50 mAP50-95 lr0 lr1 lr2



- **從行尾逆向擷取 float tokens**
- 若偵測到 epoch token like 12/499，且尾端 float >= 7，取倒數第 7~4 當作 P/R/mAP50/mAP50-95（略過 lr）
- 需容忍 ...、0.346G 等 token







### **C) 去重與排序**





- 同一 epoch 可能多筆（例如每個 epoch 印多次），採「同 epoch 取最後一筆」
- 依 epoch 排序







## **4. 衍生計算（Data-only）**







### **4.1 steps_per_epoch**





- steps = ceil(dataset_size / batch_size)

- 若 batch_size 未提供：

  

  - 嘗試從 log 中解析 --batch-size 或固定值（允許使用者 CLI 指定）
  - 否則不做 obj_per_step（仍可做其他）

  







### **4.2 obj_per_step（近似正規化）**





- obj_per_step = obj / steps_per_epoch

  （注意：這是近似，用於比較尺度，不聲稱是「每 step 的真 loss」，但能抓出明顯尺度錯覺）







### **4.3 rolling gains**





- gain_10(t) = mAP(t) - mAP(t-10)（不足則 None）

- gain_50、gain_100 同理

- plateau 判斷（可配置）：

  

  - 若 gain_100 < 0.005 → plateau
  - 或連續 K 次低於門檻 → 深 plateau

  







### **4.4 EMA 平滑**





- ema(t) = α*m(t) + (1-α)*ema(t-1)
- α 預設 0.1，可 CLI 調整







### **4.5 best-relative**





- best_epoch = argmax(mAP)
- rel(t) = mAP(t) - mAP(best_epoch)







### **4.6 mAP vs obj correlation**





- Pearson corr（需足夠點且無 None）
- 同時輸出 scatter 圖（x=obj 或 obj_per_step, y=mAP）







## **5. 圖表清單（輸出規格）**





每張圖輸出到 out/plots/：



1. map50_compare.png：mAP50 vs epoch（多 run）＋ target 線
2. obj_loss_compare.png：obj loss vs epoch
3. box_loss_compare.png、cls_loss_compare.png（可選合併）
4. map50_diff_bars.png：指定 baseline 的差值柱狀（例如 StrA - AntiConf）
5. rolling_gain.png：gain_10/gain_50/gain_100（每個 run 可各一張或同圖多線）
6. best_relative.png：mAP - best_mAP
7. map_vs_obj_scatter.png：散點（每 run 不同 marker）
8. precision_recall_compare.png（可選）





> 注意：圖上要標註 run_name、epoch 範圍、best 值與 epoch（用文字框）。





## **6. 報告輸出（summary.md）**





包含：



- 實驗列表（run 名稱、來源檔案、bs、epochs）

- 每個 run 的 KPI 表（best mAP、best epoch、final mAP、final obj/box/cls、plateau 起點）

- Pairwise diff 表（StrA - AntiConf、StrA - 1B1H）

- 一段「診斷結論」模板：

  

  - 1B4H obj 是否早平
  - mAP 是否仍在慢爬
  - rolling gain 是否低於門檻
  - mAP vs obj 是否相關（若不相關，代表 obj 不是主要瓶頸）

  







## **7. 可選模組：BN 漂移檢查（checkpoint only）**





若提供 best.pt 與 last.pt：



- 讀取 state_dict

- 對所有 BN 的 running_mean、running_var：

  

  - 計算 L2 diff、平均 abs diff、最大 abs diff

  

- 輸出：

  

  - bn_drift.json
  - bn_drift.md（列出 top-20 漂移最大 layer）

  







## **8. CLI 設計**





建議提供單一入口：

```
python -m yolo_diag \
  --runs runs.yaml \
  --out out \
  --baseline 1B4H_AntiConf \
  --target-map50 0.4353 \
  --ema-alpha 0.1 \
  --plateau-th 0.005
```



### **runs.yaml**

###  **範例**



```
dataset_size: 118287
target_map50: 0.4353
runs:
  - name: 1B1H
    path: /path/to/1b1h/results.txt
    batch_size: 128
  - name: 1B4H_AntiConf
    path: /path/to/anti/results.txt
    batch_size: 64
  - name: 1B4H_StrategyA
    path: /path/to/stra/train.log
    batch_size: 64
```



## **9. 專案檔案結構（建議）**



```
yolo_diag/
  __init__.py
  cli.py
  parse.py
  align.py
  metrics.py
  plots.py
  report.py
  bn_drift.py   # optional
tests/
  test_parse_results.py
  test_parse_log.py
  fixtures/
    sample_results.txt
    sample_train.log
pyproject.toml (或 requirements.txt)
```



## **10. 測試計畫（Data-only 測試）**







### **單元測試**





- parser 能正確解析：

  

  - results.txt
  - log 含 ...
  - log 尾端含 lr0/lr1/lr2
  - epoch token 12/499

  

- 對齊：

  

  - epoch 交集 / 聯集（缺值用 NaN 不中斷）

  







### **端到端測試**





- 用 fixtures 跑一次 CLI，確認：

  

  - out/summary.md 存在且含 KPI 表
  - out/plots/*.png 至少 6 張
  - metrics.csv 欄位齊全

  





------





# **Claude Code 執行任務拆解（可直接當 TODO）**





1. 建立 yolo_diag package + CLI

2. 實作 robust parser（行尾 float 擷取法）

3. 實作 align + 衍生計算（rolling gain / EMA / best-relative / obj_per_step）

4. 實作 matplotlib 繪圖模組（每張圖單獨輸出）

5. 實作 report generator（summary.md + CSV）

6. 加入測試 fixtures 與 pytest

   7)（可選）BN drift 模組（讀 best/last checkpoint）





------





# **你要怎麼用它（建議工作流程）**





1. 把三個 run 的 results.txt 或 train.log 準備好

2. 寫一個 runs.yaml（上面範例）

3. 跑 CLI → 直接得到 out/summary.md + 圖

4. 用 summary 判斷：

   

   - 1B4H obj 是否真的「早平」
   - plateau 起點在哪
   - mAP vs obj 是否相關（若不相關，下一步就該解凍/調 BN/看 per-head）

   





------



